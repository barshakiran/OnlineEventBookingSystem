

@model OnlineEventBookingSystem.Models.LoginViewModel
@{
    ViewBag.Title = "Login";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Login</title>
</head>
<body>
    @using (Html.BeginForm())
    {
        @Html.ValidationSummary(true)
    <div>
        <h2>Login </h2>
    </div>

        <div style="border: solid 1px #ccc; padding: 30px 0 30px 30px; border-radius: 5px;
        width: 350px; margin: auto; display: table;">
            <table>

                <tr>
                    <td valign="top">
                        @Html.LabelFor(model => model.User_Name)
                    </td>
                    <td>
                        @Html.EditorFor(model => model.User_Name)
                        <div style="height: 20px;">
                            @Html.ValidationMessageFor(model => model.User_Name, "", new { @class = "text-danger" })
                        </div>
                    </td>
                </tr>
                <tr>
                    <td valign="top">
                        @Html.LabelFor(model => model.User_Password)
                    </td>
                    <td>
                        @Html.EditorFor(model => model.User_Password)
                        <div style="height: 20px;">
                            @Html.ValidationMessageFor(model => model.User_Password, "", new { @class = "text-danger" })
                        </div>
                    </td>
                </tr>

                <tr>

                    <td height="10" colspan="2">@Html.HiddenFor(model => model.User_Id)</td>
                </tr>
                <tr>

                    <td height="10" colspan="2">@Html.HiddenFor(model => model.IsAdmin)</td>
                </tr>

                <tr>
                    <td>
                    </td>
                    <td>
                        <input type="submit" value="Login" />
                    </td>
                </tr>
            </table>
        </div>


        <div>
            @if(Session["UserName"]== null)
            { 
            @Html.ActionLink("Click here to Register", "Registration")
            }
        </div>
    }
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/jqueryval")
</body>


</html>
